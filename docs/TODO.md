# TODO — 待落地清单

> 记录模拟验证结论对应的业务落地事项，防止上下文丢失。
> 规则：业务代码改动一律**待真机验证后**再实施；模拟器是业务代码的建模镜，不是反向约束。

## 背景

cacheprobe 模拟验证（2026-08-09，DeepSeek 价 0.02/1/2）：
- 批量模式每章成本 ¥0.09-0.10 vs 单章 ¥0.24-0.28（省 60-66%），真实日志方向一致
- 批量质量短板：第 2-N 章无审稿、无写后自检；补齐方案 = 三章一轮批内检查（白金方法论），成本仅 +6.1%
- 当前业务代码（auto-inject + 技能注入 + 批量循环）**无真机数据**，所有数字来自模拟器

## 待办清单

### P1. 真机验证 auto-inject 缓存表现（前置，所有业务落地的前提）

- [ ] 跑真实会话（mimo-v2.5 / DeepSeek 任一）
- [ ] 对照 `D:\Goink\goink.log` 的 `UPDATE model_usage`：累计 hit/(hit+miss)、每轮 miss 增量、首轮全 miss 量
- [ ] 与模拟器单章/批量场景对比，有出入则修正模拟器建模（服务端缓存行为可能与字节前缀假设不同），**业务代码不动**
- [ ] 关注两条告警：`前缀变化，缓存可能失效`（agent.go:321-328）、`全量缓存未命中`（tokens.go:224）

### P2. 批量模式：批内三章一轮检查（checkKind=2 建模，模拟已验证）

- [ ] 业务代码：批量门禁流程（batchGatePlays 对应的业务实现）write 循环内，每 3 章插入：
      `set_phase("review")` → run_subagent 审最近 3 章 + 修复 + get_chapter_list 字数复查 → `set_phase("write")` 回写
- [ ] 门禁配置零改动：review 白名单已有 run_subagent；阶段回退已支持（phase_gate.go:380 visited 回退）
- [ ] 预期成本：+6.1%/章（¥0.0915 → ¥0.0971，仍比单章省 64.7%）
- [ ] 注意：set_phase("write") 会重复注入 write 技能（已计入成本）；batch review 段配置无 auto_skill_injection（切 review 不注入）

### P3. 批量大小建议（模拟验证：批内越大越省，上限看上下文窗口）

- [ ] 批量推荐 5-10 章/批（3 章是最贵档 ¥0.119/章；10 章 ¥0.084/章）
- [ ] 不拆多批：批边界新窗口成本（2批×3章比单批6章贵 24%）
- [ ] 模拟"批量 10 章 + 三章一轮"的压缩触发点（上下文压力），确定实际推荐上限

### P4. 技能注入去重（可选优化，待真机确认后评估）

- [ ] 现状：agent.go:512 `injectPhaseSkills` 每次 set_phase 无条件注入，每章重复注入相同技能全文（~9.2K miss/章，占 miss 约 10%）
- [ ] 方案：状态机记录已注入技能（`OnSkillInjected` 已记录），同阶段重复注入时跳过
- [ ] 预期：每章省 ~9.2K miss（约 12% miss）
- [ ] 风险：改业务代码，须真机验证前后对照后再实施

### P5. 模拟器已知差异（数字口径级，不影响结构结论）

- [ ] thinking 用阶段均值（真实逐次波动）
- [ ] 正文用目标字数 + 正态波动（真实逐章波动）
- [ ] set_phase reminder 内容硬编码（真实为 StatusString 动态 JSON）
- [ ] maintain 查询结果固定大小（真实随 DB 状态增长；全量 vs 分次 maintain 结论依赖此近似，已验证方向：统一 maintain 更省）

## 结论速查

| 方案 | 每章成本 | 结论 |
|---|---|---|
| 批量现状（攒批统一审） | ¥0.0915 | 基准，第 2-N 章零审稿 |
| 批量 + 三章一轮批内检查 | ¥0.0971 | **推荐**：质量节奏对齐白金 + 成本 +6.1% |
| 批量 + 每章自审 | ¥0.1050 | 过度密集，不推荐 |
| 批量 + 每批完整门禁流程 | ¥0.1110 | maintain 重复，不推荐 |
| 单章模式 | ¥0.2751 | 最贵，质量粒度最细 |
